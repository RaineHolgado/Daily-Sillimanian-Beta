import 'package:flutter/material.dart';

class InboxDetailView extends StatelessWidget {
  const InboxDetailView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Inbox Detail Screen"),
    );
  }
}